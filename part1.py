"""
Fix the code below so that it RETURNS the area of the triangle with the base and height values that are passed in (Hint: there may be some lines that you need to take away.)
"""

def trianglearea(base, height)
  base = input("Enter a base: ")
  height = float(input("Enter a height: "))
  print(base*height/2)